- name: Print Parameter from Parameter Store
  hosts: localhost
  connection: local
  tasks:
    - name: Print Parameter
      ansible.builtin.debug:
        msg: Parameter - {{ lookup('amazon.aws.aws_ssm', 'sample_sample', region='us-east-1') }}